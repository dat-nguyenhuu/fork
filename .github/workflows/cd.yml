name: cd-pipeline

on:
  workflow_dispatch:
    inputs:
      image-version:
        description: 'Image version'
        required: true
        default: 'latest'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Pull Docker image
        run: docker pull datnh99/demo-image:${{github.event.inputs.version}}

      - name: Deploy to GitHub environment
        id: run_container
        run: docker run -d -p 8080:8080 datnh99/demo-image:${{github.event.inputs.version}}

      - name: Docker container
        run: docker ps -a

#      - name: Wait for container to start
#        run: sleep 10
#
#      - name: Get container IP
#        id: get_ip
#        run: |
#          CONTAINER_IP=$(docker inspect -f '{{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}}' ${{ steps.run_container.outputs.container_id }})
#          echo "::set-output name=ip::$CONTAINER_IP"
#
#      - name: Check
#        run: curl http://${{ steps.get_ip.outputs.ip }}:8080/demo